{
  "name": "road-to-math",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "test": "npm run build && npm run test --workspace shared && npm run test --workspace server",
    "validate:content": "npm run validate:content --workspace shared",
    "start": "npm run start --workspace server",
    "start:server": "npm run start --workspace server",
    "preview": "npm run preview --workspace client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
